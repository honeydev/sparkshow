include "ddocker-reference.conf"

db = {
    host = "localhost"
    database = "test_db"
    username = "test"
    password = "test"
    port = 5446
}

docker {
  globalReuse = false
}

jwt {
    secret = "test_secret"
}
